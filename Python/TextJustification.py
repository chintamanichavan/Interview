class TextJustification(object):
    def __init__ (self, words: List[str], maxWidth: int) -> List[str]):
        self.words = words
        self.maxWidth = maxWidth
    
    def text_justification(self, words: List[str], maxWidth: int) -> List[str]:
        
        for i in :
            for j in

words = ["Chintamani","Chavan","likes","to","code"]
maxWidth = 16
t = TextJustification(words,maxWidth)
t.text_justification(words,maxWidth)
